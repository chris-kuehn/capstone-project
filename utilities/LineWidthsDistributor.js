import lineWidthRandomizer from "./LineWidthRandomizer";

export default function lineWidthsDisributor() {
  return [
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
    lineWidthRandomizer(),
  ];
}
